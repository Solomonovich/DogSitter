{
  "name": "dogsitter-firestore-rules-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Firestore Security Rules unit tests for DogSitter (allow-legit + deny-attacker per finding).",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project demo-dogsitter --config ../firebase.json \"mocha\"",
    "test:ci": "npm test"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.14.1",
    "firebase-tools": "^13.23.1",
    "mocha": "^10.7.3"
  }
}
