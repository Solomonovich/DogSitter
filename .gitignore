# Xcode
build/
DerivedData/
*.xcuserstate
*.xcworkspacedata
xcuserdata/

# Swift Package Manager
.build/
.swiftpm/

# Node — ignore deps everywhere and the stray ROOT-level npm files only.
# Subdirectory tooling (firestore-tests/, functions/) keeps its own package.json tracked.
node_modules/
/package.json
/package-lock.json

# Generated
SECURITY_AUDIT.md

# OS
.DS_Store
